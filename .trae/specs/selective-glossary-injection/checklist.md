# AI 翻译工作流 Token 优化 - 验收检查清单

## 优化 1：选择性术语注入（Selective Glossary Injection）

### 术语筛选功能
- [ ] `filterGlossary()` 函数已实现并正确工作
- [ ] 支持大小写不敏感匹配（如 "burg" 匹配 "Burg"）
- [ ] 支持词干匹配（如 "burgs" 匹配 "Burg"）
- [ ] 兜底机制正确（筛选结果 < 5 条时返回完整术语表）
- [ ] 筛选函数性能良好（处理 30+ 单元时响应迅速）

### 术语筛选集成
- [ ] `prepare()` 函数正确调用 `filterGlossary()`
- [ ] 生成的批次文件包含 `glossary` 字段
- [ ] `glossary` 字段只包含当前批次相关的术语
- [ ] 术语筛选后 token 消耗减少 70-80%

## 优化 2：TM 启发式过滤（TM Heuristic Filtering）

### TM 过滤功能
- [ ] `filterTmHints()` 函数已实现并正确工作
- [ ] 基于 `context_tag` 进行评分（相同得 2 分）
- [ ] 基于 `file` 进行评分（相同得 1 分）
- [ ] 混合排序策略正确（按评分降序，截断到 top-20）
- [ ] 保留单词重叠匹配作为补充（权重降低）

### TM 过滤集成
- [ ] `prepare()` 函数正确调用 `filterTmHints()`
- [ ] 生成的批次文件包含 `tm_hints` 字段
- [ ] `tm_hints` 字段只包含当前批次相关的 TM 条目
- [ ] TM 过滤后 token 消耗减少 60-70%

## 优化 3：Prompt Caching 支持

### 翻译规则文档更新
- [ ] `.trae/rules/localization.rules.md` 已更新
- [ ] 移除了"读 `i18n/glossary.json` 拿术语表"的步骤
- [ ] 移除了"读 `i18n/tm.json` 的最近 20 条译文作风格锚点"的步骤
- [ ] 添加了术语表和 TM 自动注入的说明

### Prompt Caching 文档
- [ ] 说明了静态内容（CONTEXT.md、规则文档）应设置缓存标记
- [ ] 提供了 API 调用示例（如 Anthropic 的 `cache_control`）
- [ ] 说明了缓存失效处理机制

## 批次文件结构

### 批次文件生成
- [ ] 批次文件格式正确（JSON 结构完整）
- [ ] 批次文件包含 `glossary` 字段（筛选后的术语）
- [ ] 批次文件包含 `tm_hints` 字段（过滤后的 TM 条目）
- [ ] 批次文件大小合理（相比优化前显著减小）

## 性能验收

### Token 消耗
- [ ] 术语相关 token 消耗减少 70-80%
- [ ] TM 相关 token 消耗减少 60-70%
- [ ] 批次文件总大小减少 60% 以上
- [ ] 会话初始化 token 消耗显著降低
- [ ] 整体 token 消耗减少 60-75%

### 翻译质量
- [ ] 术语翻译准确性未受影响
- [ ] TM 一致性未受影响
- [ ] 未出现因术语缺失导致的翻译错误
- [ ] 兜底机制在需要时正确触发

## 兼容性验收

### 向后兼容
- [ ] 现有翻译流程不受影响
- [ ] 批次文件可被现有 subagent 正确解析
- [ ] 收集流程（collect）正常工作

### 边界情况
- [ ] 空批次正确处理（无单元时不报错）
- [ ] 单单元批次正确处理
- [ ] 包含特殊字符的源文本正确处理
- [ ] 包含占位符的源文本正确处理（如 `${variable}`）

## 代码质量

### 代码规范
- [ ] 代码符合项目编码规范
- [ ] 函数命名清晰（filterGlossary、filterTmHints）
- [ ] 注释完整（说明筛选逻辑、兜底机制、评分策略）
- [ ] 无 console.log 调试语句

### 错误处理
- [ ] 读取 glossary.json 失败时有错误处理
- [ ] 读取 tm.json 失败时有错误处理
- [ ] 批次文件写入失败时有错误处理
- [ ] 边界情况有适当的错误提示

## 文档完整性

### 代码文档
- [ ] 函数注释说明输入输出
- [ ] 关键逻辑有注释说明
- [ ] 兜底机制有注释说明
- [ ] TM 评分策略有注释说明

### 用户文档
- [ ] 翻译规则文档已更新
- [ ] 批次文件格式说明已更新（如需要）
- [ ] Prompt Caching 使用说明已添加

## 最终验收

- [ ] 所有功能验收通过（术语筛选、TM 过滤、Prompt Caching）
- [ ] 所有性能验收通过（token 消耗减少 60-75%）
- [ ] 所有兼容性验收通过
- [ ] 所有代码质量验收通过
- [ ] 所有文档完整性验收通过
- [ ] 端到端测试成功（准备批次 → 翻译 → 收集）
- [ ] 性能基准测试完成，有明确的量化数据
